#include <stdio.h>
/**
 * main - prints the alphabet in lowercase and then in uppercase
 *
 * Return:(0)
 *
*/

int main (void)
{
int lett;
for (lett = 'a'; lett <= 'z'; lett++)
put char("lett");
for (lett = 'A'; lett <= 'Z'; lett++)
put char ("lett");
put char ("\n")
return (0);
}
