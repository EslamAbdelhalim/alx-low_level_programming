#include <stdio.h>
/**
 * main - prints the alphabet in lowercase and then in uppercase
 *
 * Return:(0)
 *
*/

int main (void)
{
int lett;
for (lett = 'a'; lett <= 'z'; lett++)
putchar("lett");
for (lett = 'A'; lett <= 'Z'; lett++)
putchar("lett");
 putchar("\n");
return (0);
}
